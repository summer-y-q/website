const fs = require("fs");

const process = require("process");

const path = require("path");

const PROJECT_DIR = path.join(__dirname, '..', 'src', 'views');

const CONFIG_DIR = path.join(__dirname, '../config');

const JSON_OBJECT = require("../config");

